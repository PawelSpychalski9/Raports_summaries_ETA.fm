Tool based on OpenAI API for generating raports from docs files

The script requires the use of the OpenAI API key, the key used in the code is not valid

To run the script, it is necessary to install the libraries used in the code by typing in the terminal: 
pip install -r requirements.txt
